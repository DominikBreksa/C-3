	#ifndef DISPLAY_H
		#define DISPLAY_H

		#include <cmath>
		
		#include <glad/glad.h>
    	#include <GLFW/glfw3.h>

		void ChangingColourFlutuation(const float& prev, const float& alfa, const float& time);

	#endif